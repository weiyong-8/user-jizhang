# 用户记账 —— 产品文档与项目规范

> 本文档是「用户记账」项目的总文档，由 Claude Code 维护。每次技术/产品决策都会记录到这里，请随时查阅。

## 一、项目概述

| 项目 | 内容 |
|---|---|
| 项目名称 | 用户记账 |
| 产品类型 | 桌面记账应用 |
| 运行平台 | Windows 电脑 + Mac 电脑 |
| 一句话定位 | 随手记录每一笔人民币花销，按两级分类整理，随时查看账单与消费统计 |
| 目标用户 | 项目发起人本人（非技术背景，界面必须简单易用） |
| 数据原则 | 数据只保存在用户自己的电脑上，不上传任何服务器 |

## 二、核心功能（V1.0）

1. **记一笔花销**
   - 输入金额（人民币，精确到分）
   - 选择分类：一级大类 → 二级小类
   - 日期默认当天，可修改
   - 备注选填
2. **账单列表**
   - 按日期倒序查看所有记录
   - 可按月份、分类、关键字筛选
3. **编辑与删除**
   - 任何一笔记录都可以修改或删除（删除需二次确认）
4. **分类管理**
   - 内置默认两级分类（见第三节）
   - 支持新增、改名、删除自定义分类
   - 删除分类前检查是否有历史账单使用该分类，有则提示
5. **月度统计（简单版）**
   - 每月总支出
   - 各大类支出金额与占比
   - 点击大类可查看其下二级小类明细
6. **数据备份**
   - 一键导出备份文件，可再导入恢复

### 后续版本候选（待用户决定是否做）
- 收入记录（当前版本只记支出）
- 每月预算与超支提醒
- 多设备云同步

## 三、支出分类设计（两级）

一级大类与二级小类如下，内置进 APP，用户可自行增删改：

| 一级大类 | 二级小类 |
|---|---|
| 餐饮 | 早餐、午餐、晚餐、外卖、零食饮料、买菜食材、聚餐请客 |
| 交通 | 公交地铁、打车、加油充电、停车费、火车高铁、飞机、汽车保养维修 |
| 购物 | 服饰鞋包、日用品、数码产品、家用电器、美妆护肤、其他购物 |
| 居住 | 房租、房贷、水电气费、物业费、家居装修、维修 |
| 娱乐 | 电影演出、游戏、运动健身、旅游度假、会员订阅、其他娱乐 |
| 医疗健康 | 看病买药、体检、牙科眼科、保健品 |
| 教育学习 | 书籍资料、培训课程、学费、考试报名 |
| 人情往来 | 红包礼金、孝敬长辈、请客送礼 |
| 通讯网络 | 话费流量、宽带、软件服务 |
| 金融保险 | 保险费、手续费利息、投资理财 |
| 其他 | 其他杂项 |

## 四、技术选型（决策记录）

### 决策 1：开发框架 —— ✅ 已确定（2026-08-20）
- **候选方案**：
  - Tauri：安装包约 10MB，启动快、省内存，界面用网页技术制作；技术较新。
  - Electron：最成熟（微信电脑版、QQ、VS Code 均基于它），安装包 100MB 以上、较占内存。
  - Flutter：界面精致、将来可扩展到手机，桌面版历史较短，安装包 30~50MB。
- **用户选择**：**Tauri**
- **理由**：个人记账是"小而美"的工具，轻快比什么都重要；开发与维护由 Claude 负责，技术较新的风险可控。

### 决策 2：数据存储方式 —— ✅ 已确定（2026-08-20）
- **候选方案**：
  - 本机 SQLite 数据库：稳定、查询快、方便统计，完全离线。
  - 普通文本/表格文件：可直接打开看，但记录多了会慢、易损坏。
  - 云端同步：可多设备同步，但需服务器费用、需联网、有隐私顾虑。
- **用户选择**：**本机 SQLite 数据库**
- **理由**：个人记账最通用的做法；数据不上传，符合隐私原则。

### 决策 3：界面技术 —— ✅ 已确定（2026-08-20）
- **候选方案**：React（生态最大、组件最丰富）、Vue（中文资料多）、Svelte（轻量简洁）、不用框架（不推荐）
- **用户选择**：**React**
- **理由**：现成的界面组件最丰富，开发效率高、界面质量好。

### 决策 4：数据库读写组件 —— ✅ 已确定（2026-08-20）
- **候选方案**：rusqlite（最成熟、引擎随应用打包）、sqlx（现代但复杂）、Tauri 官方 SQL 插件（数据逻辑易散乱）
- **用户选择**：**rusqlite**
- **理由**：个人记账标配，简单可靠；数据库引擎随应用打包，Windows/Mac 无需额外安装。

### 决策 5：界面组件库 —— ✅ 已确定（2026-08-20）
- **候选方案**：Ant Design（组件最全、中文生态好）、手写样式（轻量但慢）、MUI（西式风格）
- **用户选择**：**Ant Design**
- **理由**：表格、表单、日期选择等现成组件成熟，适合记账这类工具软件，界面美观统一。

### 产品决策 1：页面布局 —— ✅ 已确定（2026-08-20）
- **候选方案**：左侧菜单栏（桌面软件经典结构）、顶部标签页（更紧凑但功能多会拥挤）
- **用户选择**：**左侧菜单栏**
- **理由**：以后功能（分类管理、统计、备份）多了也放得下，入口一目了然。

### 产品决策 2：删除被使用分类的规则 —— ✅ 已确定（2026-08-20）
- **候选方案**：禁止删除并提示（最安全，不改动账单数据）、确认后自动移入「其他」（方便但会改动账单）
- **用户选择**：**禁止删除并提示**
- **理由**：不会悄悄改动任何账单数据；需要时先把账单改到别的分类或删除账单，再删分类。

### 决策 6：统计图表库 —— ✅ 已确定（2026-08-20）
- **候选方案**：ECharts（最成熟、交互好、中文资料多）、Ant Design Charts（与界面组件同门）、Recharts（轻量但功能少）
- **用户选择**：**ECharts**
- **理由**：国内最流行、图表类型全、交互流畅；桌面应用不在乎体积。

### 产品决策 3：备份方案 —— ✅ 已确定（2026-08-20）
- **保存位置**：系统弹窗选择（默认文件名"用户记账备份-日期.jzbackup"）；候选方案为固定存文档夹。
- **导入方式**：覆盖式恢复（先校验文件有效性，再二次确认，完整替换当前数据）；候选方案为合并导入。
- **理由**：符合"换电脑后恢复数据"的主要场景，逻辑简单可靠。

### 产品决策 4：应用图标 —— ✅ 已确定（2026-08-20）
- **用户选择**：定制图标（蓝底白色 ¥ 符号的圆角图标，与界面主色一致）
- **备注**：以后可随时更换。

### 决策 7：Windows 安装包制作方式 —— ✅ 已确定（2026-08-20）
- **候选方案**：GitHub 云端自动打包（免费、无需 Windows 电脑装工具）、Windows 电脑本地打包（需装环境）、先只出 Mac 版
- **用户选择**：**GitHub 云端自动打包**
- **理由**：零成本、无需在 Windows 电脑上配置环境，产出标准 NSIS 安装包。

## 五、项目协作规则（全程遵守，最重要）

### 规则 1：技术决策必须由用户拍板（用户特别要求）
- 项目发起人不懂编程，**在技术方面无法提供具体要求**。
- 因此，凡是涉及技术的事项（选型、实现方式、架构、工具等），Claude 必须：
  1. 列出 2~4 个候选方案；
  2. 用通俗语言解释每个方案是什么、优点是什么、缺点是什么（少用术语，必须用时要做解释）；
  3. 给出 Claude 自己的推荐和理由；
  4. **由用户做最终决定**，Claude 不得自行拍板。
- 用户决定后，将候选方案、用户选择及理由记录到本文档第四节。

### 规则 2：沟通方式
- 全程中文交流。
- 解释技术问题时通俗易懂、多用比喻；不堆砌术语。
- 定期汇报："做了什么、为什么、下一步做什么"，并请用户确认。

### 规则 3：产品决策
- 功能增减、界面设计等产品问题，Claude 提建议，用户确认后方可实施。
- 不擅自增加或删减已确认的功能。

### 规则 4：开发流程
- 每完成一个阶段，请用户试用确认后再进入下一阶段。
- 修改用户已确认的内容前必须先征得同意。

## 六、项目里程碑（草案，待用户确认）

1. ~~确定技术方案（框架 + 数据存储）~~ ✅ 已完成（2026-08-20：Tauri + 本机 SQLite）
2. ~~搭建项目骨架，做出可运行的窗口~~ ✅ 已完成（2026-08-20，首次编译启动成功）
3. ~~实现"记一笔花销"功能~~ ✅ 已完成（2026-08-20，用户试用通过）
4. ~~实现账单列表 + 编辑删除~~ ✅ 已完成（2026-08-20，用户试用通过）
5. ~~实现分类管理~~ ✅ 已完成（2026-08-20，用户试用通过）
6. ~~实现月度统计~~ ✅ 已完成（2026-08-20，用户试用通过）
7. ~~实现数据备份导入导出~~ ✅ 已完成（2026-08-20，用户试用通过）
8. 打包 Windows / Mac 安装包，装机试用 🔨 进行中（2026-08-20）

## 七、开发环境说明

- **云端仓库**：https://github.com/weiyong-8/user-jizhang（私有仓库；推送 main 分支会自动触发 Windows 打包流水线）
- **GitHub 账号**：weiyong-8（gh CLI 已授权，含 workflow 权限；推送前需先开启代理 127.0.0.1:7897 并 export https_proxy）
- **项目代码目录**：`/Users/weiyong/记账app/user-jizhang/`（本文档所在文件夹）
- **启动方式**：在项目文件夹下执行 `./dev.sh`（脚本会强制使用新版 Node 24，避免旧版 Node 14 干扰）
- **已安装环境**：Node 24（nvm 安装，默认版本已设为 24）、Rust 1.97（Homebrew 安装）、Xcode 命令行工具
- **Rust 下载加速**：已配置国内镜像 rsproxy（在 `~/.cargo/config.toml`，可随时移除）
- **注意**：用户的 VSCode/终端若在旧环境中启动，PATH 里可能仍是 Node 14；所有 npm 相关命令建议统一加前缀 `export PATH="$HOME/.nvm/versions/node/v24.19.0/bin:/opt/homebrew/bin:$PATH"` 或直接用 `./dev.sh`
